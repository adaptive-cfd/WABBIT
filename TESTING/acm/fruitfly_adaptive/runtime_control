# This is wabbit's runtime control file
# Stop the run but makes a backup first
# Memory is properly dealloacted, unlike KILL
#       runtime_control=save_stop;

[runtime_control]
runtime_control=nothing;
